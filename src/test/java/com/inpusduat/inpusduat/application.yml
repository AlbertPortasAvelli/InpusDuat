spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/inpusduat_test
    username: inpusduat
    password: inpusduat123

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true

  flyway:
    enabled: true
    locations: classpath:db/migration

  elasticsearch:
    uris: http://localhost:9200